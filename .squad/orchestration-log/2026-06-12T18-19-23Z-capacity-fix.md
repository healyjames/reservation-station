# Orchestration Log — Capacity Fix

**Timestamp:** 2026-06-12T18:19:23Z  
**Coordinator:** Scribe  
**Batch:** Rolling occupancy capacity model fix

---

## Summary

Coordinator took direct ownership of the implementation after agent exploration stalled. The batch aligned backend reservation enforcement, frontend guest-limit behaviour, admin copy, and test coverage around a rolling concurrent-occupancy model.

## Delegation / Ownership

- **Coordinator:** direct code changes, final integration, and `BUSINESS_LOGIC.md`
- **Sean:** `src/routes/reservations.ts` blocked-times capacity check and `POST /api/reservations` concurrent-window guard
- **Twinkie:** `Step1Form.tsx`, `BookingApp.tsx`, `useAvailability.ts`, `Step1Form.test.tsx`, `GeneralSettings.tsx`
- **Neela:** `test/index.spec.ts` concurrent-window POST coverage

## Outcome

- `GET /api/blocked-times` now evaluates `max_covers` instead of `max_guests`
- `POST /api/reservations` now rejects bookings by overlapping occupancy, not same-day summed covers
- Booking widget no longer fetches or consumes `daily-capacity`
- `Step1Form` derives its effective guest ceiling from `max_guests` / `max_covers`
- Admin copy now labels `max_covers` as **Max capacity**
- Tests were rewritten around concurrent-window capacity scenarios
- `BUSINESS_LOGIC.md` documents the rolling occupancy model at the repo root

## Notes

Decision inbox merged into `.squad/decisions.md`; inbox files removed.
