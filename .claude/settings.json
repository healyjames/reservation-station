{
  "enabledPlugins": {
    "typescript-lsp@claude-plugins-official": true
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file_path=$(echo \"$TOOL_INPUT\" | jq -r '.file_path'); if [[ \"$file_path\" =~ \\.(ts|tsx|js|jsx|json|css|md)$ ]]; then npx prettier --write \"$file_path\" 2>/dev/null; fi"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": ["Read", "Edit(.claude/**)", "Write(.claude/**)"]
  }
}
